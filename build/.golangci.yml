run:
  deadline: 10m
  issues-exit-code: 1

linters:
  enable-all: true
  disable:
    - funlen
    - godox
    - wsl
    - gomnd

issues:
  exclude:
    # Sometimes goerr113 is too annoying
    - do not define dynamic errors, use wrapped static errors instead